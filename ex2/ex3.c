#include <stdio.h>
int main() {
	double x, sum = 0;
	scanf("%lf", &x);
	sum+=x;
	scanf("%lf", &x);
	sum+=x;
	scanf("%lf", &x);
	sum+=x;
	scanf("%lf", &x);
	sum+=x;
	scanf("%lf", &x);
	sum+=x;
	printf("%lf\n", sum/5);
};
